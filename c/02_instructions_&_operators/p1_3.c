#include <stdio.h> 

int main () {
    //Q.3   // Q. step by step evaluation of 3*x/y-z+k?
       int x = 2, y = 3, z = 3, k = 1 ;
       int value= (3 * x / y - z + k );
       printf(" Answer is %d\n", value);
    
    return 0 ;
}