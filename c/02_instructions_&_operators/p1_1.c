#include <stdio.h>
#include <math.h>
int main()
{
  //Q.1 
        int a;// b = a;
        int b=a;
        int x= 3^2; // bitwise code operator not power
        int v= pow(3,2);
        char dt= '21';
        printf("v is %d \n", v);
        printf("x is %d\n",x);
        
      return 0;
}